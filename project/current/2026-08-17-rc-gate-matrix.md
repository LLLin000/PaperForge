# PaperForge M4 Release Candidate Gate Matrix

**Date:** 2026-08-17  
**Candidate SHA:** `6ef0dfe9`  
**Branch:** `rc/6ef0dfe9` (frozen candidate ref)
**Release issue:** [#81 — Cut all live control-center paths to new owners in N+1](https://github.com/LLLin000/PaperForge/issues/81)  
**Owner state:** `open` + `ready-for-human`; release/tag/package publication is not authorized.

## Purpose and decision rule

This is the single M4 acceptance matrix. Every RC claim must attach a command, artifact, live observation, or owner decision to one row here. M4 is validation, not feature development.

- **BLOCKER:** data loss/corruption, wrong authority, silent fallback, false state, unrecoverable execution, install/upgrade/rollback failure, or broken CLI/machine contract. Fix, then rerun the affected row and all dependent rows.
- **NON-BLOCKER:** cosmetic, ergonomic, performance, or post-RC architecture debt. Record it in the finding log; do not mutate the candidate.
- A row is **PASS** only when the complete path is exercised at the stated boundary. Unit coverage alone is evidence, not live-chain acceptance.
- For every mutation discovered during M4, record: **re-OCR / rebuild / embed requirement + old-user impact**. The default for validation-only work is `none / none / none`.

## Frozen candidate snapshot

| Field | Value | Evidence | Status |
|---|---|---|---|
| Candidate source tree | `6ef0dfe99bf8ad3017b4fd3c4dc396a2034a9c11` | Product candidate remains frozen; checkout/CI-only blocker repairs are `c463ffaf`, `a2fa6216`, and `14899168` | FROZEN |
| RC manifest | `project/current/rc-manifests/2026-08-17-6ef0dfe9.json` | Machine-readable environment snapshot; candidate worktree clean at full SHA | PASS — recorded |
| Python package version | `1.5.15` | `paperforge/__init__.py`; `pyproject.toml` | OBSERVED |
| Python requirement | `>=3.11` | `pyproject.toml` | OBSERVED |
| Plugin manifest | `1.5.15`, Obsidian `>=1.11.4` | `paperforge/plugin/manifest.json` | OBSERVED |
| Plugin bundle SHA-256 | `e83921a2488d05621221e4bd47ddd1fe259df3b930c46e3af7da8e47ce63033f` | Candidate-built `paperforge/plugin/main.js` copied before restoring generated output | OBSERVED |
| Plugin package SHA-256 | `package.json` `825f5fe5230d4c3d1bca979ed06528a8f13d062eca76fc8310b914dd3907af53`; `package-lock.json` `b185bb82a3ef9c0fc9668f58d14b745234e302f60bfb09eb0286f2d084b0f0ff` | Candidate worktree package inputs | OBSERVED |
| Plugin runtime | Node `>=22` | `paperforge/plugin/package.json`, CI plugin job | OBSERVED |
| Hosted CI validation ref | `148991688692e21aaefd75090e431e93a068eb17` | CI run [#32040776725](https://github.com/LLLin000/PaperForge/actions/runs/32040776725): all 14 jobs passed, including macOS unit/J-matrix, plugin, E2E, OCR regression, and `All Checks Passed` | PASS |
| Probe/read-model schema | `2` | probe contract tests and `paperforge/lineage.py` | OBSERVED |
| Vector identity | `VECTOR_IDENTITY_VERSION=1` | `paperforge/embedding/substrate.py` | OBSERVED |
| Production census | `958 = 938 OCR/retrieval current + 19 nopdf + 1 pdf_missing (4VPYIVAS)` | `indexes/recovery-census-final.json` | PASS — frozen |
| Vector materialization | `935 current + 3 no_content`; 20 excluded (`19 nopdf + 1 pdf_missing`) | recovery/embed final gate record | PASS — frozen |
| Serving | `retrieve "cartilage repair"` returned real hits | recovery/embed serving smoke | PASS — frozen |

## Unique acceptance matrix

| ID | Category | Acceptance invariant / question | CLI, call-chain, or state-machine check | Evidence | Status | Change impact if repaired |
|---|---|---|---|---|---|---|
| #81-01 | #81 owner cutover | Bare `setup` exclusively reaches the canonical setup engine; legacy TUI is inactive | Run bare setup on a fresh vault; verify argv → CLI dispatcher → canonical setup service → config/pointer publication; repeat to prove idempotence | `tests/test_cli_dispatch.py`; fresh `setup --modular --agent opencode --json` initial + repeat on `C:/Users/Lin/AppData/Local/Temp/pf_rc_fresh_6wb_xmn1`; `config.validate`; `probe installation` | PASS — canonical modular setup and repeat both rc 0; canonical schema v2 and `installation.ready` after repeat | No re-OCR/rebuild/embed; old users get canonical setup path |
| #81-02 | #81 owner cutover | Plugin dispatch uses the verified Managed Runtime identity and fails closed when stale/non-ready | Exercise plugin action → managed pointer read → child process; remove/stale pointer and prove no ambient Python fallback | Managed-runtime tests; real fresh/upgrade/offline smoke required | PENDING — Python CLI lifecycle passed; plugin managed-runtime stale-pointer/browser evidence remains required | No data rebuild; old users receive explicit setup/repair instead of fallback |
| #81-03 | #81 owner cutover | Plugin credential reads use SecretStorage; headless CLI keeps explicit env/`.env` contract | Exercise plugin credential resolution and headless CLI separately; inspect targeted child env and non-targeted install/probe env | credential tests; #81 implementation evidence; live provider smoke from audit venv | PARTIAL PASS — credential authority tests and live Windows Credential Manager provider smoke passed; plugin/browser credential path still needs runtime evidence | No re-OCR/rebuild/embed; old plaintext plugin reads remain unavailable |
| #81-04 | #81 owner cutover | Verified Python 3.10 becomes `needs_repair`; new/repair runtime requires 3.11+ | Feed 3.10, 3.11, missing, and stale pointer states through status/setup/repair; assert no silent acceptance | `pyproject.toml` requires `>=3.11`; `tests/test_runtime_pointer.py`, `tests/test_runtime_repair.py`, setup/config suite; candidate manifest | PARTIAL PASS — 130 focused setup/config/runtime tests passed and manifest records Python 3.12.10 live / 3.14.0 static runtimes; complete pointer-state live matrix remains required | No corpus rebuild; old 3.10 users need repair |
| #81-05 | #81 owner cutover | Configuration writes are schema-v2 `vault_config` only; v1 is read-only fallback | Run `setup`, `set-config`, migration, and rerun; inspect file diff and schema type; prove v1 input is not rewritten as v1 | `tests/test_config.py`; fresh repeat; existing-vault legacy config → `config migrate` → validate; rollback restore | PASS — legacy input was explicitly `migration_required`, migration produced schema v2, repeat setup stayed valid; no silent legacy read/write | No re-OCR/rebuild/embed; old v1 configs migrate on write |
| #81-06 | #81 owner cutover | Plugin maintenance rendering consumes backend fields directly; frontend does not classify live rows | Probe → wire envelope → renderer; feed unknown/stale/action permutations and assert rendered action/severity comes from backend | maintenance/probe/plugin tests; prior real-browser smoke | PARTIAL PASS — backend/probe and plugin test gates passed; browser evidence exists for the frozen report surface, but this candidate's full maintenance action journey remains pending | No materialization change; old users see backend-derived labels |
| #81-07 | #81 owner cutover | Vector build-state writes use SQLite only; legacy JSON is read-only rollback input | Run build/stop/resume/status; trace writes and compare SQLite `build_state` with legacy JSON mtime/content | shadow/rebuild tests; static writer scan; disposable existing-vault global embed | PARTIAL PASS — disposable existing-vault global build published shadow DB and serving remained current; complete writer/legacy-mtime audit remains required | Existing vectors preserved; no re-OCR/rebuild; embed only if a blocker changes materialization |
| #81-08 | #81 owner cutover | Snapshot and maintenance-cache freshness/schema gates reject stale/incompatible data | Inject old schema, stale timestamp, and malformed cache; run probe/UI and assert `unknown`/refresh, never ready | `tests/test_lineage.py`, `tests/test_chroma_migration.py`, `tests/embedding/test_embed_status_identity.py`, runtime/probe tests → **113 passed, 1 warning**; stale-cache UI injection pending | PARTIAL PASS — backend lineage/vector stale and unknown gates pass; candidate still needs stale/malformed cache injection through the UI boundary | No OCR/rebuild/embed; old caches are recomputed |
| #81-09 | #81 owner cutover | Upgrade/restart/rollback/offline/stale-cache/failed-migration paths never silently select an old owner | Exercise each transition through the real CLI/plugin boundary; record selected interpreter, config writer, credential source, and state owner | Fresh setup/repeat; existing legacy migration; config rollback fail-closed/restore; offline OCR evidence; restart lineage/retrieve | PARTIAL PASS — fresh and existing lifecycle chains passed; failed migration and config rollback failed closed without materialization hash changes; offline provider failure was explicit; managed-runtime/stale-cache/plugin rows remain open | No data rebuild unless a migration blocker is found; old users retain rollback path |
| #81-10 | #81 owner cutover | Release-N behavior remains recoverable by reinstalling N during the N+1 support window | Install candidate, preserve rollback artifact/pointer, reinstall prior version, verify vault/config/index remain readable; owner defines support window | Disposable config rollback simulation only; release artifact/reinstall evidence and owner support-window decision required | OWNER GATE — real prior-release reinstall is not claimed; keep #81 open until owner accepts the N+1 artifact/window | No automatic re-OCR/rebuild/embed; rollback must preserve materialization |
| REC-01 | Recovery | Final census is mutually exclusive and frozen | Compare census totals and categories; no item belongs to two terminal classes | `indexes/recovery-census-final.json` | PASS | Documentation-only; none / none / none |
| REC-02 | Recovery | OCR, retrieval, and materialization integrity agree for all 938 current papers | Probe lineage + integrity census; any mismatch is a blocker | recovery final gate record | PASS | Validation-only; none / none / none |
| REC-03 | Recovery | `nopdf` is a valid terminal URL-only state; `pdf_missing` is blocked source state | Probe both fixtures and assert distinct reason/detail/action; reconcile must not emit OCR/embed for either | 4VPYIVAS semantic fix; no-content/nopdf tests | PASS | No re-OCR for `nopdf`; PDF recovery only for 4VPYIVAS; no embed for excluded items |
| REC-04 | Recovery | Vector eligibility excludes `nopdf`/`pdf_missing` and accepts only retrieval-current indexable content | Run global/scoped eligibility and compare `935 current + 3 no_content`; no OCR-status shortcut | `test_embed_eligibility.py`; final embed gate | PASS | No re-OCR/rebuild; embed only for newly eligible content |
| REC-05 | Recovery | Serving is fail-closed on missing/stale lineage and real on current lineage | Run retrieval smoke plus stale/missing reader-gate cases; verify false hits are dropped | retrieval smoke; reader-gate tests | PASS — current smoke; stale-path evidence in RC gate | No corpus mutation; re-embed only if identity changes |
| REC-06 | Safety | Destructive PaperForge operations cannot recursively delete an empty/root path | Run destructive-delete scanner, trash validation tests, and prune dry-run; no production destructive action | `scripts/check_no_destructive_delete.py` → `no-destructive-delete: ok`; trash/prune tests | PASS — static scanner passed; no production destructive action run | No re-OCR/rebuild/embed; old users get fail-closed error/trash restore |
| M1-01 | M1 OCR control | OCR submission has durable `batch_id` and provider/materialization state is distinguishable | `ocr run/status/resume --batch`; inspect meta, batch observer, and local probe without provider calls | `cac9b611`, `9178d654`, `bb76e3f0`; live canary `C:/Users/Lin/AppData/Local/Temp/pf_vault_kz7puslg` | PASS — keyring credential available; real provider job completed; fresh lineage is OCR current and retrieval current before embed | No re-OCR for validation; existing jobs reattach by batch |
| M1-02 | M1 OCR control | Lock acquisition is exclusive with stale recovery; Ctrl+C detaches rather than corrupting work | Start competing controller, stale lock, and interrupt; verify job remains resumable and rc is 130 | `tests/test_ocr_control_plane.py`, `tests/cli/test_ocr_progress_contracts.py` → **49 passed, 1 skipped**; live interruption not run | PARTIAL PASS — stale/live lock, resume, submission exclusion, and cooperative cancel are covered; real Ctrl+C canary remains an evidence gap | No duplicate OCR submission; old users can resume |
| M1.1-01 | M1.1 invariant | Submission marker is written before POST; `submitting` is excluded from blind resume; provider jobs are bounded | Kill in pre/post-submit windows; inspect marker and resume candidate set; assert no double submission | `b6bf4e17`, `9889b973`; control tests | PASS — focused evidence | No re-OCR unless provider ambiguity is owner-resolved; conservative exclusion protects old users |
| M1.1-02 | M1.1 invariant | `no_content` is terminal-satisfied across reconcile, preflight, progress, and resume | Seed no-content paper; run reconcile/resume/status; assert no embed resume and no false deficit | no-content tests; M3 eligibility tests | PASS — focused evidence | No OCR/rebuild/embed; old no-content rows remain searchable only where content exists |
| M2-01 | M2 action contract | Availability (`available/unavailable/busy`) is orthogonal to applicability (`needed/noop/blocked/not_applicable`) | Run `action preflight` for all/papers scopes across missing key, busy, satisfied, blocked, and needed states | `1a7430b5`; preflight tests; live fresh/existing no-op and unavailable credential probes | PARTIAL PASS — focused contract tests plus live needed/no-op/unavailable states; complete busy/blocked/needed CLI matrix remains pending | No materialization change; old users receive explicit remediation |
| M2-02 | M2 action contract | `per_key`, summary, and successful keys have one source of truth | Force mixed success/noop/blocked/failed/cancelled outcomes; derive summary and keys from per-key only | settlement tests; `744d73cf`; live success/offline failure transcripts | PARTIAL PASS — focused settlement tests and live OCR success/offline failure evidence; mixed all-outcome subprocess matrix remains pending | No re-OCR/rebuild/embed; only reporting changes if blocker found |
| M2-03 | M2 action contract | Runner pipeline is complete: lookup → scope validation → preflight → confirmation → handler → PFResult → follow-up | Exercise unknown action, invalid scope, missing confirmation, mismatched confirmation, handler error, KeyboardInterrupt | `tests/test_action_registry.py`, T7 journey, direct CLI, live confirmation path | PARTIAL PASS — focused runner gate, direct CLI, live OCR success, explicit invalid embed scope, and structured PFResult passed; cancellation-specific subprocess path remains pending | No materialization change; old users get structured errors/rc130 |
| M2-04 | M2 machine contract | CLI exit codes and JSON/NDJSON streams are stable and stdout-clean | Run success, invalid input, unavailable, handler failure, cancel, and stream EOF/second-terminal cases; assert rc and exactly one terminal | Local suite + direct minimal-vault transcript + live disposable outcomes | PARTIAL PASS — local CLI suite **120 passed, 1 skipped**; live OCR success, offline per-key failure, migration-required, invalid scope, and restored serving were exercised; live cancel remains pending | No re-OCR/rebuild/embed; old machine consumers depend on this contract |
| M2-05 | M2 authority | Action registry is the policy/handler/preflight authority; no command/argv policy leaks | Validate registry invariants and inspect action wire; unknown actions fail closed | `tests/test_action_registry.py` | PASS — focused gate | No materialization change |
| M3-01 | M3 embed | Embedding eligibility asks only direct parent truth: retrieval current + indexable content | Compare selector outputs against probe lineage; reject `ocr_status == done` as a candidate authority | `tests/test_embed_eligibility.py`; `f6e5db35`, `30cbfc65` | PASS — focused gate | No re-OCR/rebuild; only eligible papers embed |
| M3-02 | M3 embed | Build and resume use the same selector and progress denominator | Run global and papers-scope paths with eligible/no-content/not-ready mix; compare candidate and denominator sets | `test_build_and_resume_share_one_selector`; `fd75deaf` | PASS — focused gate; live scoped smoke pending | No re-OCR/rebuild; embed only missing eligible vectors |
| M3-03 | M3 boundary | Actions/services/materialization never import `paperforge.commands.*` | Run architecture boundary test and static import scan; allowlist must remain empty | `tests/test_architecture_boundaries.py`; `cbc66579` | PASS — 0 exemptions | No re-OCR/rebuild/embed |
| M3-04 | M3 boundary | CLI is a thin adapter; Action calls Service directly; structured result remains truth | Trace `action run embed.*` and direct CLI invocation; confirm no Action→CLI recursion; record stdout relay as deferred presentation debt only | `paperforge/actions/registry.py` handlers import/call `paperforge.services.embedding.run_embedding_build`; `paperforge/commands/embed.py` remains parse → request → service → render | PARTIAL PASS — Action→Service direct path and zero command imports are verified; service stdout relay/JSON extraction remains deferred non-blocking presentation debt for post-RC, before Goal/Ensure | No re-OCR/rebuild/embed; old users see same execution |
| RC-01 | RC execution | Complete CLI situation matrix: success, invalid args/key, unknown action/key, unavailable/busy, blocked, remote/provider failure, cancel, resume, offline | Execute real subprocesses with disposable vaults; capture argv, rc, stdout, stderr, PFResult/NDJSON terminal, and final probe state for every situation | Local suite + direct disposable vaults `C:/Users/Lin/AppData/Local/Temp/pf_vault_mezld1me`, `pf_vault_kz7puslg`, `pf_vault_erkn0ylq` | PARTIAL PASS — credential-available OCR → memory → embed → retrieve succeeded; invalid/unknown/unavailable paths and offline failure were exercised; offline resume returned no unfinished papers with `submission_state=submitting` excluded; live Ctrl+C/cancel remains pending | No re-OCR/rebuild/embed; only a blocker repair may alter behavior |
| RC-02 | RC execution | Complete user chain: input → CLI/plugin dispatch → preflight → action/service → materialization → probe → UI/read result | Run fresh vault and existing vault through setup → sync → OCR canary → memory → embed scoped/global → retrieve; restart and repeat | Fresh `pf_rc_fresh_6wb_xmn1`; existing `pf_rc_existing_269fev7d`; live keyring/PaddleOCR/SiliconFlow evidence | PARTIAL PASS — fresh setup/repeat, sync, OCR, memory, global embed, retrieve, restart; existing legacy migration, setup/repeat, sync, OCR, memory, global embed, retrieve, restart; plugin/UI and true scoped embed remain pending | No re-OCR/rebuild/embed; validation-only |
| RC-03 | RC execution | Every mutating action has complete preflight, postcondition, and re-observation | For each action, assert preflight decision, settlement, materialization commit, fresh probe, and UI/read projection agree | OCR/embed live canary transcripts + action/probe integration evidence required | PARTIAL PASS — fresh/existing OCR and embed have preflight/PFResult/fresh lineage/retrieve evidence; setup/plugin/UI and lifecycle action rows remain pending | No re-OCR/rebuild/embed; validation-only |
| RC-04 | RC execution | Static release gate is clean and reproducible | Run version sync, Ruff F821–F823, Python contract suites, plugin vitest/typecheck/build, architecture/destructive-delete checks, workflow/checksum/version checks | Frozen source tree `6ef0dfe9`: local version sync, Ruff, Python unit/J-matrix/protocol/E2E/audit/OCR regression, plugin 408/408/typecheck/build, and bundle hashes passed; hosted validation ref `14899168` passed all 14 CI jobs, including macOS after the explicit Homebrew Python shim; deterministic collector had complete Python + TypeScript coverage but remained `incomplete` under #134 for six unresolved authority/dynamic-call rules and two advisory adapter-wrapper findings | PARTIAL PASS — hosted CI is green; unresolved deterministic-collector facts remain post-RC architecture-tooling debt under the documented skip policy | No re-OCR/rebuild/embed; rerun static gates only if candidate source changes |
| RC-05 | RC execution | No unapproved release side effect occurs during RC | No tag, PyPI publish, GitHub Release, production deploy, or issue close; #81 remains open | Git status, issue state, owner decision | PASS — policy; owner release decision pending |

## Required evidence bundle before owner decision

1. **CLI evidence:** disposable-vault command transcript for RC-01, including rc/stdout/stderr/terminal event and post-probe state for every situation.
2. **Call-chain evidence:** one trace each for setup, OCR, action/runner, embed, and retrieve; identify the authority at every boundary.
3. **State-authority evidence:** probe lineage/build-state/credential/runtime reads and all writers; prove no stale cache or legacy writer wins.
4. **Pre/post evidence:** preflight result, execution settlement, commit point, fresh observation, and UI/read projection for each mutating path.
5. **Static evidence:** reproducible local RC-04 commands plus hosted CI `All Checks Passed` on validation ref `148991688692e21aaefd75090e431e93a068eb17` (run `#32040776725`); the deterministic collector is complete-coverage but ineligible under #134's unresolved-evidence policy and remains recorded as post-RC debt.
6. **Owner gate:** #81 release decision, version/tag choice, publication authorization, and N+1 support-window/rollback acceptance.

## Finding log

| Finding | Classification | Evidence | Decision / repair | Re-run impact | Old-user impact |
|---|---|---|---|---|---|
| `2c5f9147` static gate blockers | BLOCKER — repaired | Version sync passed; Ruff found missing `hashlib` in `materialization/ocr.py` and missing `sys` in `worker/ocr.py`; focused OCR/action/materialization gate then passed **128 passed**. Two stale OCR state-machine test assumptions were corrected: fixture pipeline path injection and intentional `fatal_error` terminal state. | Committed as `2c5f9147`; superseded as candidate head by the workflow-only `6ef0dfe9` | none / none / none | Prevented runtime NameError; no user-visible materialization change |
| `6ef0dfe9` publish runtime mismatch | BLOCKER — repaired | `plugin/package.json` and `.nvmrc` require Node 22; CI used Node 22 but `publish.yml` used Node 20. Node contract assertion, version sync, plugin 408/408, and typecheck passed after the fix. | Changed publish workflow to Node 22; candidate is now frozen | none / none / none | Release-builder correctness only; no existing-user data impact |
| `CLI-LOCAL-2026-08-17` contract evidence | EVIDENCE — local only | `pytest tests/cli tests/test_cli_worker_dispatch.py tests/test_cli_paths.py tests/test_e2e_cli.py tests/test_action_registry.py tests/test_t7_journey.py` → **120 passed, 1 skipped**. This validates subprocess JSON/text/NDJSON/error contracts and runner transitions, not the complete live-vault situation matrix. | Keep RC-01 partial; collect real success/failure/cancel/offline/resume transcripts before owner gate | none / none / none | No user impact; validation only |
| `CLI-DIRECT-2026-08-17` disposable smoke | EVIDENCE — local only | Minimal vault direct calls: `status --json` rc 0; `action list --json` rc 0; `probe installation --json` rc 0; `retrieve --json` returns `vector not_built` rc 1; unknown action returns `action.unknown` rc 2; invalid context returns rc 1; preflight returns explicit unavailable credential state. | Keep RC-01/M2-04 partial; add credential-available and cancellation/offline/resume evidence | none / none / none | No user impact; validation only |
| `RC-RUNTIME-2026-08-17` | EVIDENCE — live disposable only | Audit venv reported both `ocr` and `embedding` credentials available from Windows Credential Manager. Real PaddleOCR job completed; `probe lineage` reached OCR current → retrieval current → vector current after build; `retrieve "testing setup"` returned two real figure hits. | No product mutation; retain as runtime evidence and continue fresh/existing lifecycle gates | none / none / none | No user impact; validation-only |
| `RC-OFFLINE-2026-08-17` | EVIDENCE — live disposable only | Explicit local OCR endpoint refused connections. Run produced three bounded upload attempts, per-key `failed`, meta `fatal_error` with diagnostic transport error and batch marker; `ocr resume --batch` returned no unfinished papers, excluding the `submission_state=submitting` crash window. | No repair; cancellation remains an evidence gap. Do not treat outer command completion `rc=0` as per-key success; consume `summary`/`per_key`/probe together. | none / none / none | No user impact; disposable vault only |
| `RC-ENV-2026-08-17` | EVIDENCE GAP — environment | Main local venv is Python 3.14.0 but its installed keyring source contains null bytes and reports `backend_error`; live provider smoke therefore used the verified audit venv (Python 3.12.10). Candidate worktree stayed clean and candidate code was unchanged. | Keep separate runtime identities in the manifest; complete managed-runtime installation gate before owner decision. | none / none / none | No existing-user impact observed; test-environment setup only |
| `RC-LIFECYCLE-2026-08-17` | EVIDENCE — disposable lifecycle only | Fresh setup repeated successfully; existing copied vault with legacy top-level config returned `migration_required`, explicit `config migrate` produced schema v2, and setup/sync/OCR/memory/embed/retrieve/restart completed. Restoring legacy config failed closed; restoring v2 preserved SHA-256 for DB/OCR materialization files and lineage remained 3/3 current. Offline provider test remained explicit per-key failure with bounded retries and no blind resume. | Keep #81-02/#81-06/#81-08/#81-10 owner/runtime gates open; no candidate code change | none / none / none | Disposable vaults only; no existing-user impact |
| `RC-PLUGIN-2026-08-17` | EVIDENCE — local plugin gate | `paperforge/plugin`: `npm test` → **23 files / 408 tests passed**; `npm run typecheck` passed; `npm run build` passed and generated bundle was restored without candidate source changes. | Keep managed-runtime stale-pointer and browser maintenance journeys open; no code repair | none / none / none | No existing-user impact; validation-only |
| `RC-STALENESS-2026-08-17` | EVIDENCE — local stale-state gate | Lineage, migrated-vector config drift, vector identity, and runtime pointer tests returned **113 passed, 1 warning**; stale/unknown states remain fail-closed. | Keep UI cache injection and managed-runtime browser paths open; no code repair | none / none / none | No existing-user impact; validation-only |
| `RC-CI-2026-08-17` | PASS — hosted CI; architecture collector post-RC debt | Run [#32040776725](https://github.com/LLLin000/PaperForge/actions/runs/32040776725) at `14899168`: **14/14 jobs passed**, including macOS unit/J-matrix, plugin tests, E2E/audit, OCR regression, L4b architecture gate, and `All Checks Passed`. The preceding run exposed two release-blocking environment defects: orphaned `.open-design/source` gitlink (`c463ffaf`) and macOS Python without `enable_load_extension` (`a2fa6216`); explicit Homebrew Python symlinks in `14899168` resolved the latter. The collector had complete Python + TypeScript coverage but remained ineligible for six unresolved active rules and two advisory wrapper findings, as designed by #134's skip policy. | Keep product source tree `6ef0dfe9` frozen; classify unresolved collector facts as post-RC architecture debt, not a materialization or release-runtime defect. | none / none / none | No existing-user impact; CI environment and audit evidence only |
