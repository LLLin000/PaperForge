---
phase: 61-annotation-storage-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 61-01
files_modified:
  - paperforge/annotation/schema.py
  - tests/unit/annotation/test_schema.py
autonomous: true
requirements:
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "Annotation schema version is independent from memory schema version"
    - "meta table records schema_version=1 after ensure_schema"
    - "annotations table includes source/library scope, paper association, content fields, position JSON, sync/read-only state, timestamps, and soft-delete field"
    - "sync_queue exists as future write-back placeholder but no write-back implementation is added"
    - "ensure_schema is idempotent and does not drop existing rows"
  artifacts:
    - path: "paperforge/annotation/schema.py"
      provides: "Annotation DB schema lifecycle"
      exports: ["ANNOTATION_SCHEMA_VERSION", "ensure_schema", "get_schema_version"]
    - path: "tests/unit/annotation/test_schema.py"
      provides: "Schema creation/version/idempotency tests"
  key_links:
    - from: "tests/unit/annotation/test_schema.py"
      to: "paperforge/annotation/schema.py"
      via: "schema lifecycle imports"
      pattern: "from paperforge.annotation.schema import"
    - from: "tests/unit/annotation/test_schema.py"
      to: "paperforge/annotation/db.py"
      via: "connection helper imports"
      pattern: "from paperforge.annotation.db import"
---

<objective>
Define the independent annotation database schema and schema lifecycle functions.

Purpose: Give Phase 62 a safe persistence layer for imported Zotero annotations while keeping schema evolution separate from `paperforge.db`.
Output: `paperforge/annotation/schema.py` and schema tests.
</objective>

<execution_context>
@C:/Users/tan/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/tan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-annotation-storage-foundation/61-CONTEXT.md
@.planning/phases/61-annotation-storage-foundation/61-RESEARCH.md
@.planning/phases/61-annotation-storage-foundation/61-PATTERNS.md

Current analogs:
@paperforge/memory/schema.py
@tests/unit/memory/test_schema.py

Plan dependency:
@.planning/phases/61-annotation-storage-foundation/61-01-PLAN.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add failing schema lifecycle tests</name>
  <files>tests/unit/annotation/test_schema.py</files>
  <action>
    Create `tests/unit/annotation/test_schema.py` with tests for:
    1. `ensure_schema` creates `meta`, `annotations`, `sync_queue`, and `annotations_fts`.
    2. `get_schema_version` returns `0` before meta exists and `1` after `ensure_schema`.
    3. `ensure_schema` can be called twice without dropping an inserted annotation row.
    4. `annotations` table exposes required columns:
       - `id`, `paper_id`
       - `source`, `source_library_id`, `source_annotation_key`, `source_attachment_key`, `source_parent_key`
       - `source_version`, `source_modified_at`
       - `type`, `page_index`, `page_label`
       - `selected_text`, `comment`, `color`, `sort_index`, `tags_json`
       - `position_json`, `selector_json`
       - `sync_state`, `is_readonly`
       - `created_at`, `updated_at`, `deleted_at`
    5. FTS triggers index inserted selected text/comment.

    Run these tests before implementation and confirm they fail.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/annotation/test_schema.py -q</automated>
  </verify>
  <done>Schema tests fail because `paperforge.annotation.schema` is not implemented yet.</done>
</task>

<task type="auto">
  <name>Task 2: Implement annotation schema lifecycle</name>
  <files>paperforge/annotation/schema.py</files>
  <action>
    Create `paperforge/annotation/schema.py` following `paperforge/memory/schema.py`, but with an independent schema version:
    - `ANNOTATION_SCHEMA_VERSION = 1`
    - `CREATE_META`
    - `CREATE_ANNOTATIONS`
    - `CREATE_ANNOTATIONS_FTS`
    - `CREATE_SYNC_QUEUE`
    - indexes for paper/source/type/page/deleted rows
    - FTS triggers for insert/update/delete
    - `ensure_schema(conn)`
    - `get_schema_version(conn) -> int`

    Schema details:
    - Store source/library scope using generic names, not only Zotero names.
    - Keep `sync_queue` as a placeholder table for future write-back, but do not implement write-back behavior.
    - `ensure_schema` should insert `schema_version` into `meta` only when absent.
    - Do not implement `drop_all_tables` for annotations in Phase 61.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/annotation/test_schema.py tests/unit/annotation/test_db.py -q</automated>
  </verify>
  <done>Schema tests pass and schema version is independent from memory schema version.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/annotation/test_schema.py tests/unit/annotation/test_db.py -q`
</verification>

<success_criteria>
- [ ] `paperforge/annotation/schema.py` exposes schema version, `ensure_schema`, and `get_schema_version`.
- [ ] Required annotation columns exist.
- [ ] `meta.schema_version` records annotation schema version `1`.
- [ ] `ensure_schema` is idempotent and preserves existing rows.
- [ ] FTS table/triggers index annotation text/comment.
</success_criteria>

<output>
After completion, create `.planning/phases/61-annotation-storage-foundation/61-02-SUMMARY.md`.
</output>
