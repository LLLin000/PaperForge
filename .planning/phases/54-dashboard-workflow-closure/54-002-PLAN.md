---
phase: 54-dashboard-workflow-closure
plan: 002
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - AGENTS.md
  - docs/INSTALLATION.md
  - docs/setup-guide.md
autonomous: true
requirements:
  - CLEAN-01

must_haves:
  truths:
    - "The plugin-first installation path is the primary documented entry point, not CLI/pip"
    - "docs/INSTALLATION.md no longer exists as a competitor entry point"
    - "docs/setup-guide.md no longer exists as a competitor entry point"
    - "README.md documents the plugin-first path as the recommended approach"
    - "README.md no longer references deleted docs/"
    - "Developer/pip install documentation is either removed from README.md or moved to docs/dev/ if retained"
  artifacts:
    - path: "README.md"
      provides: "Main documentation entry point, plugin-first path"
      min_lines: 100
    - path: "AGENTS.md"
      provides: "Installation guide referencing the plugin path"
  key_links:
    - from: "README.md"
      to: "README.md"
      via: "No references to deleted docs/INSTALLATION.md or docs/setup-guide.md"
      pattern: "setup-guide.md|INSTALLATION.md"
    - from: "AGENTS.md"
      to: "AGENTS.md"
      via: "No references to deleted docs"
      pattern: "setup-guide|INSTALLATION"
---

<objective>
Remove competing entry-point documentation files so the plugin-first path is the only recommended way to install PaperForge.

Purpose: Users encountering PaperForge for the first time should see a single, clear installation path (plugin download -> enable -> wizard), not conflicting pip-based instructions. This closes CLEAN-01.
Output: Modified README.md, AGENTS.md; deleted docs/INSTALLATION.md, docs/setup-guide.md.
</objective>

<execution_context>
@C:/Users/Lin/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lin/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/54-dashboard-workflow-closure/54-CONTEXT.md

<interfaces>
File structure to modify:

**README.md** (at repository root):
- Lines 1-24: Header, tagline, code block "download plugin -> enable -> wizard" — keep as-is (this IS the plugin-first path)
- Lines 45-73: Installation section — currently has "Obsidian 插件（推荐）" and "命令行安装（开发者）" subsections
- Lines 171-179: "文档" table that references docs/setup-guide.md and docs/INSTALLATION.md — must update
- Lines 66-72: Developer pip install section — decision says "move to docs/dev/ if needed"

**AGENTS.md** (at repository root):
- First line: "本文档面向 **安装完成后的新用户** 和 **AI Agent**。安装步骤见 [setup-guide.md](docs/setup-guide.md) 或快速版 [INSTALLATION.md](docs/INSTALLATION.md)。"
- References to setup-guide.md and INSTALLATION.md must be removed

**docs/INSTALLATION.md**: To be deleted entirely
**docs/setup-guide.md**: To be deleted entirely
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete competing docs and update README.md</name>
  <files>docs/INSTALLATION.md, docs/setup-guide.md, README.md</files>
  <action>
    **Step 1: Delete competing files**

    Delete these files from disk (they are the old pip-first entry points):
    - `docs/INSTALLATION.md`
    - `docs/setup-guide.md`

    Use `fs.unlinkSync` or `Remove-Item` to delete.

    **Step 2: Update README.md**

    The following changes to README.md are needed:

    1. **"文档" section (lines 171-179):** Replace the table that references deleted files:

    ```
    ## 文档

    | 文档 | 内容 |
    |------|------|
    | [安装后指南](AGENTS.md) | 首次使用与工作流说明 |
    | [变更日志](CHANGELOG.md) | 版本历史 |
    | [贡献指南](CONTRIBUTING.md) | 开发环境与协作约定 |
    | [架构说明](docs/ARCHITECTURE.md) | 系统设计与数据流 |
    | [命令参考](docs/COMMANDS.md) | 完整命令速查表 |
    ```

    Remove the rows for [安装指南](docs/setup-guide.md) and [快速安装](docs/INSTALLATION.md).

    2. **Developer install section (lines 66-72):** Per CONTEXT.md decision: "Move developer install info to docs/dev/ if needed or remove entirely". Since developer install is secondary to the plugin-first path and doesn't conflict with it, either keep a minimal reference or move to `docs/dev/`. 

    **Recommended approach: Keep a minimal "Developer Install" subsection under "安装" but clearly mark it as secondary.** Replace lines 66-72 with:

    ```markdown
    ### 命令行安装（开发者）


    ```bash
    pip install git+https://github.com/LLLin000/PaperForge.git
    ```

    > 开发者安装方式主要用于调试或贡献代码。新用户请使用上方的 Obsidian 插件安装方式。
    ```

    (The headless setup command can stay since it's used by developers.)

    **Step 3: Verify no remaining references**

    Search README.md for any remaining references to:
    - `setup-guide.md`
    - `INSTALLATION.md`
    
    If any remain (especially in the 安装 section or the code block), replace or remove them.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const docsDir='docs';const files=['INSTALLATION.md','setup-guide.md'];const deleted=files.map(f=>!fs.existsSync(docsDir+'/'+f));console.log('INSTALLATION.md deleted:',deleted[0]);console.log('setup-guide.md deleted:',deleted[1]);const readme=fs.readFileSync('README.md','utf-8');console.log('README has no setup-guide ref:',!readme.includes('setup-guide.md'));console.log('README has no INSTALLATION ref:',!readme.includes('INSTALLATION.md'));console.log('README has plugin-first path:',readme.includes('下载插件') || readme.includes('download plugin'));"</automated>
  </verify>
  <done>
    docs/INSTALLATION.md and docs/setup-guide.md are deleted from disk. README.md no longer references them. The "文档" table has been updated to remove deleted file entries. README.md still presents plugin-first installation as the primary path.
  </done>
</task>

</tasks>

<verification>
1. Confirm docs/INSTALLATION.md and docs/setup-guide.md no longer exist.
2. Confirm README.md contains no references to setup-guide.md or INSTALLATION.md.
3. Confirm README.md still clearly presents the plugin-first installation path as the primary recommendation.
</verification>

<success_criteria>
1. docs/INSTALLATION.md does not exist on disk.
2. docs/setup-guide.md does not exist on disk.
3. README.md "文档" table no longer references deleted files.
4. README.md installation section starts with "Obsidian 插件（推荐）" as the primary path.
5. AGENTS.md no longer references deleted docs.
</success_criteria>

<output>
After completion, create `.planning/phases/54-dashboard-workflow-closure/54-002-SUMMARY.md`
</output>